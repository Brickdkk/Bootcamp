<Window x:Class="Bootcamp_Frontend.SuccessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="InvenPro - Sesion Iniciada" 
        Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Border Background="#FAFAFA" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3" Color="#00A878"/>
        </Border.Effect>
        
        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid Width="100" Height="100" Margin="0,0,0,30">
                    <Ellipse Width="100" Height="100" Fill="#00A878">
                        <Ellipse.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.4" Color="#00A878"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    
                    <Path Data="M 30,50 L 45,65 L 70,35" 
                          Stroke="White" 
                          StrokeThickness="5"
                          Width="60" 
                          Height="50"
                          Stretch="Uniform"/>
                </Grid>
                
                <TextBlock Text="Sesion iniciada correctamente" 
                         FontSize="28" 
                         FontWeight="Bold" 
                         Foreground="#333333"
                         TextAlignment="Center"
                         Margin="40,0,40,20"/>
                
                <TextBlock Text="Bienvenido a InvenPro" 
                         FontSize="16" 
                         Foreground="#999999"
                         TextAlignment="Center"
                         Margin="40,0,40,30"/>
                
                <Button Content="Cerrar" 
                        Width="150" 
                        Height="45"
                        Background="#00A878"
                        Foreground="White"
                        FontSize="14"
                        FontWeight="SemiBold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#009268"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
